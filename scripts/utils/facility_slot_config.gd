## 文件说明：该脚本属于设施槽位配置相关的配置资源脚本，集中维护槽位唯一标识、本地坐标、槽位标签等顶层字段。
## 审查重点：重点核对字段命名、默认值、配置含义以及它们与存档结构、规则判定之间的对应关系。
## 备注：后续如果调整字段语义，需要同步检查资源配置、序列化逻辑和所有读取方。

class_name FacilitySlotConfig
extends Resource

## 字段说明：在编辑器中暴露槽位唯一标识配置，便于策划或关卡制作者在不改代码的情况下调整该脚本行为。
@export var slot_id: String = ""
## 字段说明：在编辑器中暴露本地坐标配置，便于策划或关卡制作者在不改代码的情况下调整该脚本行为。
@export var local_coord: Vector2i = Vector2i.ZERO
## 字段说明：在编辑器中暴露槽位标签配置，便于策划或关卡制作者在不改代码的情况下调整该脚本行为。
@export var slot_tag: String = ""
## 字段说明：在编辑器中暴露要求配置，便于策划或关卡制作者在不改代码的情况下调整该脚本行为。
@export var required := false
